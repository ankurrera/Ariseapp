import 'package:flutter/material.dart';
import '../common/system_panel.dart';

class SkillPointsPanel extends StatelessWidget {
  const SkillPointsPanel({super.key});
  @override
  Widget build(BuildContext context) {
    return const SystemPanel(title: "Skills", child: Center(child: Text("Skill Points Panel Placeholder")));
  }
}